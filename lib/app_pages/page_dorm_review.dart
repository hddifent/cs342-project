import 'package:flutter/material.dart';

class DormReviewPage extends StatelessWidget {
  const DormReviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Dorm Review Page..."));
  }
}